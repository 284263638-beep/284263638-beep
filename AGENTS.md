# Prototype Instructions

## Durable visual direction

- The current portfolio source of truth is the supplied 2026 white desktop artboards: light-gray page background, black typography, and fixed 16:9 proportional layouts. Do not revert to the earlier black version unless the user explicitly requests it again.
- Project browsing uses one continuous white portfolio page with eight borderless light-gray horizontal cards in a single column, one card per row. Opening a card shows that project's vertically scrollable work images in an overlay window; dedicated full-page project-detail sections are no longer part of the main scroll sequence.
- The About AI page has been removed. The About Me page keeps the centered copy and the eight-project thumbnail marquee from the previous version.
- About Me copy and the thumbnail marquee enter together from left to right. The thumbnails loop continuously, pause and enlarge by 30% on hover, and open their matching project galleries when selected.
- Portfolio cards are compact, approximately 30% shorter than the initial single-column version. The small `TABLE OF CONTENTS` label and the right-side `01—08` header microcopy are omitted.
- Full-height sections use mandatory page snapping so navigation cannot rest between two pages; the oversized portfolio list remains one continuous scroll section.
- A white resume page follows About Me: small `About Me` at top left, large `Curriculum Vitae` at top right, the supplied IP at bottom left, and an expandable resume list anchored at bottom right with generous left-side whitespace.
- Portfolio card headings, supporting copy, and Watch controls use a larger, more legible scale while the cards remain compact.
- All desktop pages retain one proportional composition across viewport sizes rather than switching to an alternate small-screen layout.
- The home and ending pages use the same `instant-4` pointer-responsive IP animation from `head-turn-demo` as their last/back visual layer at 97.2% canvas scale. Each page starts the native source segment from 4–6 seconds when it first enters the viewport, then switches to the 43-frame nonlinear pointer-tracking interaction. All video and frame assets are copied into `public/assets/portfolio-2026/head-turn-instant4/`, so the portfolio never depends on the separate port-4173 demo remaining online. Their top layers remain crisp native HTML typography and interface shapes from the supplied `资源 6@2x.png` and `资源 10@2x.png` references. The home art layer remains at 90% scale and the ending art layer at 90.25%; the light-gray section background remains full size. The ending page retains a visible crosshair pointer for the head-turn interaction.
- The standalone head-turn demo uses the latest supplied native 3840×2160 source and preserves its complete 16:9 video frame and character proportions. It stays text-free, plays the source segment from 4–6 seconds on entry, then switches to pointer tracking. The interactive asset uses the inspected 61-frame contact sheet, a nonlinear `ANGLE_KEYS` calibration, and 43 full-resolution native source frames. Do not key, crop, isolate, add transparency, crossfade frames, or otherwise alter their picture content. Predecode the full frame pool to the fixed on-page 1280×720 display size during the intro, draw through an opaque canvas, use a 24ms direction response and at most two source-frame steps per display update so the character catches the pointer quickly without a decode queue, giant 4K texture, or blank flash.
- The home and ending main English titles are 20% smaller and use a visibly 20%-lighter weight than the first native reconstruction, with each title group shifted upward by 3px. The top-right `Brand Design` control is a full rounded dark pill on both pages. The home name/contact block is shifted downward by 5px, and the ending copy omits its former short horizontal divider.
- The large home and ending title lines are horizontally widened by 10% without changing height and use slightly increased letter spacing. The home title's adjacent `<2026>` moves downward by 2px. `Curriculum Vitae` and `Design Project` are also widened by 10% without changing height, anchored to their existing right and left edges respectively, with letter spacing matching the About Me heading. The two ending Chinese lines use slightly more leading.
- Subsequent micro-alignment moves the home title's adjacent `<2026>` down by another 2px, the ending title's adjacent `<2026>` up 5px and left 2px, and every right-edge card word (`ONE`—`EIGHT`) down 5px and left 2px.
- Final micro-alignment adds another 1px downward to the home title `<2026>`, another 5px upward to the ending title `<2026>`, and another 5px leftward to every right-edge card word.
- The home and ending pages each include the same top-left masking block on a dedicated layer immediately above the animated background and below the native foreground artwork. Its `#e3e5e6` fill must match the animation background, and the first top-bar label remains black. After prior alignment changes, the ending title `<2026>` receives a further 5px upward and 5px leftward adjustment.
- The home and ending artboards clip all child layers at their own page bounds so the top-left masking block can never extend into an adjacent page. Every right-edge card word (`ONE`—`EIGHT`) receives a further 5px leftward adjustment.
- The top-left masking block on both the home and ending artboards is shifted down by exactly 15px. Every right-edge card word (`ONE`—`EIGHT`) receives another 3px leftward adjustment.
- The About Me Chinese statement is `专注于成为解决业务问题的AI全链路设计管理者`, with its forced line break immediately after `的` and before `AI`.
- The three former invisible navigation hotspots on the home artboard are removed.
- The resume page contains three real work-experience blocks headed by date and role. Its left visual uses the latest supplied transparent pouting IP as a single static image; the old two-image hover swap is removed.
- Portfolio pages use white backgrounds, black text, borderless light-gray cards, generously inset content, and visibly enlarged project metadata and open controls.
- Resume experience rows omit numeric prefixes and expand one at a time; the first experience is open initially. Date and role are black, and no expand/collapse label or icon is shown. The experience list enters from bottom to top when the page appears.
- Every project card must always expose its two-digit number, project title, complete project introduction, and a high-contrast Watch/open button regardless of copy length.
- Project descriptions and Watch controls are anchored directly to the card rather than nested inside the heading content container, preventing the first card's longer introduction from being clipped. All card headings are shifted upward by exactly 3px; all card numbers and Watch controls are independently shifted downward by exactly 5px, and every project-name label is shifted downward by exactly 2px.
- Project-card open controls use the supplied `点击观看` button reference directly so its black pill, white circular arrow area, proportions, corner radius, and arrow stroke remain exact. Card numbers and titles share a baseline near the top of the content area; every description/control row is anchored to the same bottom inset, with the visible button bottom aligned to the description bottom.
- The project-list heading is `Design Project`, with uppercase `MY DESIGN` at the far right using the same small gray typographic treatment as the resume page's `About Me` label.
- Resume copy begins farther right so it never overlaps the left-side IP.
- Project 04 is `将军账号UI设计`; its cover and modal gallery use the eight supplied `将军账号ui/VI/2x` artboards in the exact user-provided order (1, 7, 8, 9, 10, 11, 12, 18).
- Project-card thumbnails use each gallery's first work image, except project 01 and project 08 retain their existing thumbnails and project 07 uses its fourth work image. Card number and project name stay on one line.
- All eight project-card thumbnail images are shifted upward by exactly 5px inside their existing media containers.
- The final image in project 04 uses the latest supplied 1x `/07_界面展示` artboard.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.
